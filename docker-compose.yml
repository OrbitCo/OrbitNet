version: "3"
services:
  server:
    container_name: orbit_net_server
    working_dir: /app
    depends_on:
      - mongo

  mongo:
    image: mongo
    container_name: orbit_net_mongo
    ports:
      - 27017
    volumes:
      - ./data/mongo/data:/data/db      
